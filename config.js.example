const path = require("path");
module.exports = {
    video_enable: 1,
    rank_enable: 0,
    user_enable: 0,
    live_enable: 1,
    data_path: path.resolve("./"),
    web: {
        "enable": true,
        "host": "0.0.0.0",
        "port": "8083"
    }
}
}